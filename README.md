This repo contains some sample TypeScript code that uses the [TypeORM](https://typeorm.io/#/) framework to connect to [CockroachDB](https://www.cockroachlabs.com/docs/stable/).

To run the code:

```shell
$ npm install
$ npm start
```
